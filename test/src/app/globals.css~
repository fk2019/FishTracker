@import "tailwindcss";

:root {
  --background: #ffffff;
  --foreground: #171717;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #6f6f6f;
  }
}

body {
  background: var(--foreground); /*change bg*/
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}
body {
    background: url('../assets/fish_p.png');
    background-size: 50%;
    background-blend-mode: multiply;
}

.menu {
    display: flex;
    flex-direction: row;
    flex-basis: auto;
    font: 12px;
    width: 100%;
    justify-content: space-between;
    border-radius: 10px;
}
.menu-button {
    cursor: pointer;
}

.sidebar {
    color: #7988;
}
flex {
    flex-direction: column;
}
.home-link
{
    color: red;
    margin: 2rem;
}

.modal {
    background:  url('../assets/fish_p.png');
}
.content {
    border-radius: 1rem;
    background: none;
}
.content-form, .text-center>p {
    background: white;
    opacity: 90%;
    border-radius: 1rem;

}
.text-bg {
    display: inline-flex;
    background: white;
    opacity: 90%;
    border-radius: 1rem;
    padding: .4rem;
    margin-top: 2rem;
    margin-bottom: .1rem;
}

hr {
    border: none;
    border-top: .1rem solid white;
    margin-top: 2rem;
}
.content-form {
    background: white;
}
.text-center>p {
    padding: .7rem;
    opacity: 70%;
}
.farm-name {
    display: flex;

    border-radius: 1rem;
    background:none;
    padding: .2rem;
    justify-content: center;
}
.farm-name>span{
    font-size: 2rem;
    color: blue;
    box-shadow: 0 0 4px rgba(0, 0, 0, 0.1);
    font-weight: 800;
}
@media screen and (max-width: 448px) {
    body {
	background: url('../assets/fish_p.png');
        background-size: cover;
    }
}
